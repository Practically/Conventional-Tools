# Copyright 2023 Practically.io All rights reserved

name: Publish Build

on:
  push: {branches: ['1.x']}
  workflow_run:
    workflows:
      - 'CI'
    types:
      - 'completed'

jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest
    outputs:
      build_output: ${{ steps.build_output.outputs.}}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 16.x

      - name: Build Application
        id: build_output
        run: >
          apt update && apt install -y p7zip-full
          yarn install
          yarn build -t linux-x64 >> $GITHUB_OUTPUT
  push:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: practically/conventional-tools

      - name: Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ${{ needs.build.outputs.build_output }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
